# Copy this to terraform.tfvars and fill values as needed

aws_region   = "us-east-1"
env          = "dev"

# must be globally unique (change this!)
bucket_name  = "vyshu-tf-demo-123456"

# EC2 toggles
create_ec2    = true
instance_type = "t3.micro"
allow_ssh     = true
